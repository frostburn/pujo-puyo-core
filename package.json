{
  "name": "pujo-puyo-core",
  "author": "Lumi Pakkanen",
  "version": "0.0.1",
  "description": "Core engine for a Puyo Puyo clone",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/frostburn/pujo-puyo-core.git"
  },
  "bugs": {
    "url": "https://github.com/frostburn/pujo-puyo-core/issues"
  },
  "homepage": "https://github.com/frostburn/pujo-puyo-core#readme",
  "keywords": [
    "Puzzle",
    "Game",
    "Puyo",
    "Puyo Puyo",
    "Mean Bean Machine"
  ],
  "module": "src/index.ts",
  "devDependencies": {
    "bun-types": "latest",
    "gts": "^5.0.1",
    "typescript": "~5.1.6",
    "@types/node": "20.4.6",
    "@types/jsdom": "^16.2.15"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "bun build src/index.ts --outdir=build --target=browser && bun build lib/svgdefs.ts --outdir=build --target=browser && bun build lib/sound.ts --outdir=build --target=browser && bun build lib/worker.ts --outdir=build --target=browser",
    "fix": "gts fix",
    "prepare": "bun run compile",
    "pretest": "bun run compile",
    "posttest": "bun run lint",
    "start": "bun x serve ."
  },
  "type": "module"
}
